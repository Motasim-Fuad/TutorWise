import 'package:flutter/material.dart';

class TutorMyActivityScreen extends StatelessWidget {
  const TutorMyActivityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("TutionMyActivity Page"),
      ),
    );
  }
}

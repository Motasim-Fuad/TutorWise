import 'package:flutter/material.dart';

class ReferrerMyRefer extends StatelessWidget {
  const ReferrerMyRefer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ReferrerMyRefer page"),
      ),
    );
  }
}

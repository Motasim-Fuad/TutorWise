import 'package:flutter/material.dart';

class RefererMyActivity extends StatelessWidget {
  const RefererMyActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("RefererMyActivity page"),
      ),
    );
  }
}

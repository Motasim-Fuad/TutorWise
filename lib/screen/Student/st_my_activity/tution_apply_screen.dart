import 'package:flutter/material.dart';

class TutionApplyScreen extends StatelessWidget {
  const TutionApplyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Tuition Apply Screen"),
    );
  }
}

import 'package:flutter/material.dart';

class StudentMyTutor extends StatelessWidget {
  const StudentMyTutor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("StudentOffers page"),
      ),
    );
  }
}
